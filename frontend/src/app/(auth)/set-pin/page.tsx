export default function SetPinPage(): JSX.Element {
  return (
    <main className="min-h-screen p-8">
      {/* TODO: Add transaction PIN setup for onboarding security. */}
      <h1 className="text-3xl font-semibold">Set PIN</h1>
    </main>
  );
}
