export default function OtpVerifyPage(): JSX.Element {
  return (
    <main className="min-h-screen p-8">
      {/* TODO: Add OTP verification UI with 6-digit input and resend timer. */}
      <h1 className="text-3xl font-semibold">OTP Verify</h1>
    </main>
  );
}
