export default function SendPage(): JSX.Element {
  return (
    <main className="min-h-screen p-8">
      {/* TODO: Add send money flow with recipient picker and amount entry. */}
      <h1 className="text-3xl font-semibold">Send</h1>
    </main>
  );
}
