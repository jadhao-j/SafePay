export default function ScanPage(): JSX.Element {
  return (
    <main className="min-h-screen p-8">
      {/* TODO: Add QR scanner and manual UPI fallback for merchant payments. */}
      <h1 className="text-3xl font-semibold">Scan</h1>
    </main>
  );
}
