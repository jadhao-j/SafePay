export default function ProfilePage(): JSX.Element {
  return (
    <main className="min-h-screen p-8">
      {/* TODO: Add profile, MFA settings, trusted devices, and security score links. */}
      <h1 className="text-3xl font-semibold">Profile</h1>
    </main>
  );
}
