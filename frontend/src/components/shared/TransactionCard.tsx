import { Card } from "@/components/ui/card";

export function TransactionCard(): JSX.Element {
  return (
    <Card>
      {/* TODO: Add transaction metadata, amount, status, and risk explanation preview. */}
      <h3 className="text-lg font-semibold">Transaction Card</h3>
    </Card>
  );
}
